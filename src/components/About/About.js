import PageTitle from '../PageTitle/PageTitle';

const About = () => {
  return (
     <PageTitle title="About" text="A simple to-do app, with lists, columns and card"/>
  );
};

export default About;